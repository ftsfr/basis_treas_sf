# Manual data

Files placed here are tracked in git and are not regenerated by the
pipeline.

- `treasury_sf_implied_rf.dta` (optional): the Siriwardane, Sunderam, and
  Wallen reference series for the Treasury spot-futures spread. When
  present, `src/test_replication.py` compares the pipeline outputs against
  it; when absent, those tests skip.
